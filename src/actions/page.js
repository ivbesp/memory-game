
export const renderStartPage = () => (
    {
        type: 'RENDER_STARTPAGE'
    }
);

export const renderGamePage = () => (
    {
        type: 'RENDER_GAMEPAGE'
    }
);

export const renderEndPage = () => (
    {
        type: 'RENDER_ENDPAGE'
    }
);

